// ExitShortcut — "Controls" tab sub-section.
// Displays the current exit shortcut (default: "Hold Escape for 3s").
// "Change" button opens a key-capture dialog for custom combos (P2).

export default function ExitShortcut() {
  return null
}
