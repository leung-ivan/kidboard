// GuestBanner — subtle non-blocking prompt shown in settings for guests.
// "Sign in to save settings across devices and unlock paid themes."
// TODO (P1): implement; hide once user is authenticated.
export default function GuestBanner() {
  return null
}
