// LoginModal — shown when a guest tries to access a paid/auth-gated feature.
// TODO (P1): wire up Apple JS SDK and Google Identity Services sign-in flows.
export default function LoginModal() {
  return null
}
