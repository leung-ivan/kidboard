// PurchaseSuccess — landing page after Stripe Checkout redirects back.
// Route: /purchase/success
// Polls GET /api/user/purchases (or listens for WebSocket event) to confirm unlock,
// then navigates back to the playground with the newly unlocked theme active.

export default function PurchaseSuccess() {
  return null
}
